# PUBLIC_INTERFACE
# REACT_APP_API_BASE is used by the frontend API client (src/api/client.js)
# to connect to the backend server. For local dev with backend on 3001:
# REACT_APP_API_BASE=http://localhost:3001
REACT_APP_API_BASE=
